package main

func main() {
	//happy path first tab
	//negative path inside of if statements (error handling)

	//functions can return n number of values, but
	//raise a flag if there are 3 values in a return
	//raise a flag if there are 2 values, but one of them isn't err

	//variable names need more detail the further down that they are used (smaller/closer scope = smaller variable names)

	// variable _ means we don't care about it

}